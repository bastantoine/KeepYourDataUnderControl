server {

    listen 80;
    root /usr/src/app/;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # location / {
    #     proxy_pass http://demo_service_front;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header Host $host;
    #     proxy_redirect off;
    # }

}